import * as React from 'react';

import { TodoListProvider } from '../../store/todoListStore';

const TodoList = () => {
  return (
    <TodoListProvider>

    </TodoListProvider>
  );
};

export default TodoList;
